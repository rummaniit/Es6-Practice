const doubleIt = (num) => num * 2
const res = doubleIt(5)
console.log(res)

const addIt = (num1, num2) => {
    const addition = num1 + num2
    const dif = num1 - num2
    const mul = addition * dif
    return mul
}
const res2 = addIt(5, 4)
console.log(res2)